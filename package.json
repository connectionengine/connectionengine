{
  "name": "connectionengine",
  "private": true,
  "license": "CAL-1.0",
  "type": "module",
  "scripts": {
    "dev": "concurrently \"pnpm --filter @connectionengine/client dev\" \"pnpm --filter @connectionengine/server dev\"",
    "build": "pnpm -r --if-present build",
    "test": "pnpm -r test",
    "certs": "mkdir -p .certs && openssl req -x509 -newkey rsa:2048 -keyout .certs/localhost.key -out .certs/localhost.cert -days 365 -nodes -subj \"/CN=localhost\"",
    "lint": "oxlint --fix",
    "format": "oxfmt --write",
    "check": "pnpm -r check && oxlint",
    "prepare": "husky"
  },
  "devDependencies": {
    "@fastify/pre-commit": "^2.2.1",
    "@types/node": "^25.9.1",
    "concurrently": "^9.2.1",
    "husky": "^9.1.7",
    "lint-staged": "^16.4.0",
    "oxfmt": "^0.53.0",
    "oxlint": "^1.68.0",
    "tsdown": "^0.22.1",
    "tsx": "^4.22.4",
    "typescript": "^5.9.3",
    "vitest": "^4.1.8"
  },
  "lint-staged": {
    "*.{js,jsx,ts,tsx}": [
      "oxlint --fix",
      "oxfmt --write"
    ],
    "*.{json,css,md,html}": [
      "oxfmt --write"
    ]
  }
}
